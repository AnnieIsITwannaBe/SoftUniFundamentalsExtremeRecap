package ArraysExercise;

import java.util.*;

public class MaxSequenceOfElements2 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String[] sequence = scanner.nextLine().split("\\s+");

        findMaxSequence(sequence);
    }

    private static void findMaxSequence(String[] sequence) {

//        int index = 0;
//        List<String> current = new ArrayList<>();
//        Map<Integer, String> sequencesFound = new HashMap<>();
//        while (sequence[index].equals(sequence[index + 1])) {
//            current.add(sequence[index]);
//           current.add(sequence[index + 1]);
//           sequencesFound.add
//    }
//}
    }
}
